The system was executed on ASP.NET Core using EF Core (Code first). 
Divided into database priject, DTO project, repository project and the main project.

The database file is created when the application starts (if it's not) and is located in TaskTracker\bin\Debug\netcoreapp3.1

To launch the application and view the logs in the console window, select the "TaskTracker" profile.
